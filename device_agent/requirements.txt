# Web Framework
Flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5

# Async Support
gevent>=24.0.0

# HTTP Requests
requests==2.31.0

# Keyboard Input (Cross-platform)
pynput==1.7.6
# Keyboard Input (Linux headless, optional)
# python-evdev>=1.6.0  # 仅在 Linux 无桌面环境下需要，使用: pip install python-evdev

# Bluetooth (Cross-platform)
# Note: Version >=0.22.0 supports pyobjc-core 10.x+
# If you have pyobjc-core 12.x, upgrade bleak: pip install --upgrade bleak
bleak>=0.22.0

# Bluetooth Classic Control (Linux - Ubuntu)
# DBus interface for BlueZ (推荐，功能最全)
dbus-python>=1.2.18

# Audio Playback Control
# VLC player (推荐，支持完整播放控制)
python-vlc>=3.0.20123

# ALSA Audio Control (可选)
pyalsaaudio>=0.9.0

# Other dependencies (if any)
# Add your other dependencies here

