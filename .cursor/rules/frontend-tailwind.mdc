---
description: Vue 前端样式使用 Tailwind v4，不用 scoped CSS
globs: **/*.{vue,tsx,jsx}
alwaysApply: false
---

# 样式约定

- 使用 Tailwind utility class 写样式，不要新增 `<style>` / `<style scoped>` 块
- 改现有组件时，优先把 `<style>` 迁到 template 的 `class` 上
- Element Plus 组件样式用 Tailwind 任意选择器，例如 `[&_.el-dialog__body]:p-0!`
- 不要写内联 `style="..."`，除非 Tailwind 无法表达（如动态 computed 值）

## Tailwind v4 写法（必须遵守）

本项目使用 **Tailwind CSS v4**，写完后对改过的文件跑 `read_lints`，**零 Tailwind 警告**再交付。

### Important 修饰符

- ✅ 后缀：`text-white!`、`bg-black/60!`、`hover:bg-blue-400!`、`disabled:cursor-not-allowed!`
- ❌ 前缀：`!text-white`、`!bg-black/60`、`hover:!bg-blue-400`

### 优先用内置 utility，少写任意值

- ✅ `z-100` ❌ `z-[100]`
- ✅ `line-clamp-2` ❌ 手写 `[-webkit-line-clamp:2]`、`[display:-webkit-box]`、`overflow-hidden` 组合
- ✅ `wrap-anywhere` / `wrap-break-word`（v4.1+）❌ 自造 `wrap-anywhere` 以外的 overflow-wrap 任意属性
- ✅ `inset-x-3`、`left-5` 等 spacing ❌ `w-[calc(100%-1.5rem)]` 等可替代的 calc
- ✅ `text-xs`、`leading-5`、`h-10` ❌ `text-[11px]` 等非必要任意字号

仅在**没有等价 utility**时才用 `[...]` 任意值（如 `[&::part(native)]:p-0`、`pb-[100%]` 正方形占位）。

### 常见场景

- 多行截断：`line-clamp-2`（需要固定高度时配合 `leading-5 h-10`）
- flex 内长文本换行：`wrap-anywhere` + `min-w-0`
- UC 等旧浏览器不支持 `aspect-ratio` 时：外层 `pb-[100%]` + 内层 `absolute inset-0`
- 覆盖 Ionic / Element Plus：`[--width:90vw]`、`[&_.el-dialog__body]:p-0!`

### 交付前检查

1. 改完 template 中的 `class` 后，对涉及文件执行 lint/诊断
2. 修复全部 Tailwind CSS IntelliSense 警告（canonical class、suggest replacement 等）
3. 不要留冗余 class（如已有 `line-clamp-2` 时不要再加 `overflow-hidden`）
